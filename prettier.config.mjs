export default {
  semi: true, // 在语句末尾添加分号
  singleQuote: true, // 使用单引号而不是双引号
  trailingComma: "es5", // 在 ES5 中有效的最后一个元素后添加逗号
  printWidth: 80, // 每行的最大长度
  tabWidth: 2, // 每个缩进级别的空格数
  useTabs: false, // 使用空格而不是制表符
  bracketSpacing: true, // 在对象字面量的大括号之间打印空格
  jsxBracketSameLine: false, // 在 JSX 中将 `>` 放在最后一行的末尾
  arrowParens: "avoid", // 仅在需要时为单参数箭头函数添加括号
};
